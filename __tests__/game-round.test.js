import GameRound from '../src/game-round.js';

describe('GameRound', () => {
  test('is defined', () => {
    expect(new GameRound()).toBeDefined();
  });

  // todo: add tests for logic introducing methods
});
