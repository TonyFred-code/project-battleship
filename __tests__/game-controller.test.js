import GameController from '../src/game-controller.js';

describe('GameController', () => {
  test('is defined', () => {
    expect(new GameController()).toBeDefined();
  });

  // todo: add tests for logic introducing methods
});
